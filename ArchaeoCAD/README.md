# ArchaeoCAD Mobile

PWA launcher for ArchaeoCAD Mobile.

Enable GitHub Pages for the repository using the branch `main` and the folder `/ (root)`. Then open:

`https://jj-martinez-garcia.github.io/ArqueoCAD/ArchaeoCAD/`

The launcher installs as a PWA and opens the current ArchaeoCAD Mobile application.